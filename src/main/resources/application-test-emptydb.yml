spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:partidos-test-emptydb
    username: test
    password: test
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2-console

  sql:
    init:
      mode: never
